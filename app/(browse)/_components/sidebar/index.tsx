import { FC } from "react";
import { Wrapper } from "./wrapper";
import { Toggle } from "./toggle";

export const Sidebar: FC = ({}) => {
  return (
    <Wrapper>
      <Toggle />
    </Wrapper>
  );
};
